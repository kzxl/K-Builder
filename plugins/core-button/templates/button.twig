<div class="kbuilder-button-wrap" style="text-align: {{ alignment|default('center') }}; margin: 1.5rem 0;">
    <a href="{{ url|default('#') }}" class="kb-btn kb-btn--{{ size|default('md') }} kb-btn--{{ style|default('primary') }}">
        {{ text|default('Click here') }}
    </a>
</div>
